{
  "name": "retail-ai-profile-api",
  "version": "1.0.0",
  "description": "Profile API Lambda — store validation, session history, cart/wishlist",
  "main": "dist/handler.js",
  "scripts": {
    "build": "node esbuild.config.js",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.600.0",
    "@aws-sdk/lib-dynamodb": "^3.600.0",
    "aws-jwt-verify": "^5.1.1"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.145",
    "@types/node": "^20.14.0",
    "esbuild": "^0.21.5",
    "typescript": "^5.5.0"
  }
}
